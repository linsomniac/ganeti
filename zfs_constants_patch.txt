# ZFS Constants to Add to Installed Ganeti

# 1. Add these disk template constants (find where other DT_ constants are defined):
DT_ZFS = "zfs"

# 2. Add this storage type constant (find where other ST_ constants are defined):
ST_ZFS = "zfs"

# 3. Update DISK_TEMPLATES to include DT_ZFS (find the DISK_TEMPLATES = frozenset([...]) line):
# Change from:
DISK_TEMPLATES = frozenset([
    DT_DISKLESS, DT_PLAIN, DT_DRBD8, DT_FILE, DT_SHARED_FILE,
    DT_BLOCK, DT_RBD, DT_EXT, DT_GLUSTER
])

# To:
DISK_TEMPLATES = frozenset([
    DT_DISKLESS, DT_PLAIN, DT_DRBD8, DT_FILE, DT_SHARED_FILE,
    DT_BLOCK, DT_RBD, DT_EXT, DT_GLUSTER, DT_ZFS
])

# 4. Update DTS_EXT_MIRROR to include DT_ZFS (find the DTS_EXT_MIRROR line):
# Change from:
DTS_EXT_MIRROR = frozenset([DT_GLUSTER, DT_RBD, DT_EXT])

# To:
DTS_EXT_MIRROR = frozenset([DT_GLUSTER, DT_RBD, DT_EXT, DT_ZFS])

# 5. Add ZFS to DISK_DT_DEFAULTS (find the DISK_DT_DEFAULTS = { section):
# Add this entry:
    DT_ZFS: {
        "pool": "pool",
    },

# Location of file to edit:
# Usually: /usr/local/share/ganeti/3.1/ganeti/constants.py
# Or: /usr/share/ganeti/ganeti/constants.py